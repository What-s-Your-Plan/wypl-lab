import tw from 'twin.macro';
import styled from 'styled-components';

const Container = styled.div`
  ${tw`
    h-full
    w-full

    flex
    flex-col

    justify-center
    px-4
    py-2
  `}
`;

export { Container };
