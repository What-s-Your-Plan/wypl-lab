import tw from 'twin.macro';
import styled from 'styled-components';

const CreateDiv = styled.div`
  ${tw`
    flex
    px-2
    gap-3
    justify-center
    content-center
    items-center
    h-9
    cursor-default
  `}
`;

export { CreateDiv };
