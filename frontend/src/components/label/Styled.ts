import tw from 'twin.macro';
import styled from 'styled-components';

const CreateDiv = styled.div`
  ${tw`
    flex
    px-2
    gap-1
    justify-center
    content-center
    items-center
  `}
`;

export { CreateDiv };
